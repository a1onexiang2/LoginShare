ext {
    compileSdkVersion = 25
    buildToolsVersion = "25.0.3"
    minSdkVersion = 16
    targetSdkVersion = 25
    versionCode = getVersionCode();
    versionName = getVersionName();
}

def getVersionName() {
    return "0.1.1"
}

def getVersionCode() {
    return 2
}